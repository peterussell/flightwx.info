# Namespaces for the FAA XML docs
# NB. It would be better to extract these from the doc (if possible) rather than
# hard coding them.
FAA_XML_NAMESPACE = { "product_set": "http://arpa.ait.faa.gov/arpa_response" }
